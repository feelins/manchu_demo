# 现有资产与端口清单（速查）

> 用途：门户收编 7 个 HTML 时的对照表。
> 更新时间：2026-09-24

---

## 1. 前端资产（7 个 HTML）

根目录：`/home/leyesi/shaopf/服务器配置相关/平台框架/平台框架/`

| 模块 | 相对路径 | 大小 | 形态 | 硬编码后端地址 |
|---|---|---|---|---|
| 古籍 OCR | `01ocr/满文古籍OCR智能识别系统_竖版_批量版 汉字识别开关.html` | — | 纯静态 | `http://localhost:1000`、`http://localhost:8080`（下拉可选，存 localStorage）；调用 `/api/recognize` |
| 满文⇄拉丁互转 | `02传统满文与拉丁满文双向转换/满文拉丁互转竖排2.html` | 54 KB | 纯静态（前端规则引擎，内置 docx 解析） | 无 | **已移植**（2026-09-24）→ `/translit/`，见 [07](./07-文字转换模块移植记录.md) |
| 满汉翻译 | `03满汉双向翻译/满汉翻译_三栏_完美版 三模型.html` | — | 纯静态 | `http://localhost:8001`（GGUF Q8_0）/ `8003`（LoRA-GGUF）/ `8005`（llama-server + LoRA，`model: "Qwen3.6-27B"`） |
| 语音合成 TTS | `04语音合成/tts加了满文.html` | 12 KB | **Jinja2 模板**（`{{ project_title }}`、`/static/style.css`、`/static/app.js`、`/static/fonts/001A-Buleku-R.ttf`） | 同源 |
| 语音识别 ASR | `05语音识别/` | **空** | 无 | 需全新开发 |
| 语料集 | `06语料集/`（0601 满文OCR训练数据、0602 满汉双向翻译训练数据） | — | 静态说明 + 样例 | 无 |
| 工作平台 · OCR 标注 | `07工作平台（需登录，不对外）/0701满文古籍OCR数据标注/manju_annotation_tool_v8AI预测_修正收集版文本框加长加文件名.html` | 107 KB | 纯静态 | `http://localhost:7860/detect`；`http://127.0.0.1:1000/api/recognize`；CDN `jszip@3.10.1` |
| 工作平台 · 双行校对 | `07.../0702满汉双行数据验证工具/文本格式校对工具.html` | 23 KB | 纯静态 | CDN `mammoth@1.4.2`（**已知极慢，建议重写**） |
| 工作平台 · 依存语法 | `07.../0703依存语法可视化/manchu_ud_annotator.html` | 47 KB | 纯前端 SVG | 无 |

其它：`关于网站的大体想法.docx`（需求）、`满语文数智化平台系统架构图.pdf`、`满语文数智化平台框架.png` / `框架 2.png`、`流程图.png`、字体 `其他版块满文字体/001A-Buleku-R.ttf`。

---

## 2. 后端服务与端口

来源：`/home/leyesi/shaopf/服务器配置相关/服务启动器.py`

| 端口 | 服务 | 启动命令 / 目录 | 模块 | 备注 |
|---|---|---|---|---|
| 8005 | 满汉翻译 llama-server + LoRA | `C:\llama-b9912-bin-win-cpu-x64\llama-server.exe -m ...\Qwen3.6-27B-q4_k_m.gguf --lora C:\lora\lora.gguf --host 0.0.0.0 --port 8005 -c 2048 -t 8` | 翻译 | 纯 CPU GGUF |
| 8003 | 满汉翻译 API (LoRA-GGUF) | `python api_server_v3_lora_gguf.py`（`...\满汉翻译模型\api`） | 翻译 | — |
| 8001 | 满汉翻译 API (GGUF Q8_0) | `python api_server_v3_llama_q8_fix.py`（同上目录） | 翻译 | 旧模型 |
| 7860 | 标注推理后端（检测） | `python inference_server_local.py`（`D:\满文训练\scripts`，`MANJU_MODEL=manju_detect_v5.onnx`） | OCR 检测 / 标注 | ONNX |
| 1000 | 标注前端服务（识别，含版面分析） | `python server.py`（`D:\ManjuOCR20`，conda `manju_layout`） | OCR 识别 / 标注 | `/api/recognize` |
| 8080 | Ubuntu 远程 OCR（本机） | `ssh leyesi@10.22.0.17` → `conda activate manju_layout` → `cd /home/leyesi/ocr_deploy && python server.py --host 0.0.0.0 --port 8080` | OCR 识别 | `ocr_deploy/server.py` 默认端口 1000；路由 `/`、`/api/recognize`、`/fonts/<path>`；日志 `/tmp/ocr_server.log` |
| 7868 | 满语语音合成 TTS | `python app.py`（`D:\manchu_TTS`，conda `manchu_tts`） | TTS | 本机副本 `/home/leyesi/shaopf/manchu_TTS/app.py`（FastAPI）：`GET /`、`POST /api/convert`、`POST /api/synthesize`、`GET /healthz` |
| 待定 | ASR | 无 | ASR | 建议 Whisper / FunASR 微调 |
| **8000** | **门户（本目录）** | `.venv/bin/python app.py`（`/home/leyesi/shaopf/manchu_demo`） | 门户 | 新增；端口不与翻译 8001/8003/8005 冲突 |

---

## 3. 数据资产（门户"语料集"卡片）

> ⚠️ 下表中的数字**来自需求文档，尚未核实**。对外展示前必须逐项核实（见方案 Q5）。

| # | 语料类型 | 文档数字 | 样例位置 |
|---|---|---|---|
| ① | 满文 OCR 训练数据 | 合成 1,430,707 条 / 10 种字体；真实古籍 8,599 条 | `06语料集/0601满文OCR训练数据/{合成数据量实例/图3.jpg, 真实古籍数据/真实古籍数据示例.pdf}` |
| ② | 满汉双向翻译训练数据 | 98,038 行 | `06语料集/0602.../all1+2+3.txt`（仅展示几十条即可） |
| ③ | 语音合成数据 | 待邵老师提供 | — |
| ④ | 语音识别训练数据 | 2,292 条 | 与 TTS 数据反向复用 |
| ⑤ | 已有古籍总量 | 529 种/套 | — |

本机语音数据目录（可用于统计真实规模）：`/home/leyesi/shaopf/Manchu_data*` 系列（v2~v8，各含 wav / TextGrid / txt）。

---

## 4. 字体

| 文件 | 位置 | 用途 |
|---|---|---|
| `001A-Buleku-R.ttf` | `平台框架/其他版块满文字体/`、`07.../0701.../专用字体/`、本目录 `static/fonts/` | 满文显示（竖排）。**待做**：WOFF2 转换 + 子集化 |
