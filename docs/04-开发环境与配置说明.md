# 开发环境与配置说明

> 目录：`/home/leyesi/shaopf/manchu_demo`
> 更新时间：2026-09-24（对应阶段 0：门户骨架已跑通）

---

## 1. 运行环境

| 项 | 值 |
|---|---|
| 系统 | Ubuntu（本机 IP `10.22.0.17`，用户 `leyesi`） |
| Python | 3.13.9（`/home/leyesi/miniconda3/bin/python3`） |
| 虚拟环境 | `manchu_demo/.venv`（`python3 -m venv .venv`） |
| Flask | 3.1.3 |
| 监听 | `0.0.0.0:8000`（`debug=True`）；端口可用环境变量 `PORT` 覆盖 |

## 2. 目录结构（当前，阶段 1 后）

```
manchu_demo/
├── app.py                # Flask 入口：create_app() + 上下文变量 + 占位路由
├── config.py             # 站点信息 / 导航 / 页脚 / 首页数据（数据驱动外壳）
├── requirements.txt      # Flask>=3.0
├── .gitignore            # .venv/ __pycache__/ *.pyc
├── .venv/
├── templates/
│   ├── base.html         # ★ 统一外壳（head/字体/主体/页脚 + block content）
│   ├── index.html        # 首页（extends base.html）
│   ├── placeholder.html  # 模块占位页（extends base.html）
│   └── partials/{header,footer}.html
├── static/
│   ├── css/design-token.css   # ★ 设计令牌（配色/字体/圆角/阴影）
│   ├── css/base.css           # 外壳 + 通用组件（机构条/导航/页脚/区块/卡片/标签）
│   ├── css/home.css           # 首页专属（Hero/流水线/数据卡/工作平台）
│   └── fonts/001A-Buleku-R.ttf
├── docs/
└── logs_app.log
```

**约定**
- 新页面一律 `{% extends "base.html" %}` 并填 `{% block content %}`；页面级样式放 `{% block extra_css %}`。
- 颜色/间距/圆角一律用 `design-token.css` 里的 CSS 变量，禁止在页面中写死色值。
- 导航、页脚、首页数据集中在 `config.py`，模板不做硬编码。

## 3. 常用命令

```bash
cd /home/leyesi/shaopf/manchu_demo

# 首次安装
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt

# 前台启动
.venv/bin/python app.py

# 后台启动（日志 logs_app.log）
nohup .venv/bin/python app.py > logs_app.log 2>&1 &

# 一键启停「OCR + TTS + 门户」三个服务（推荐）
./scripts/services.sh start|stop|restart|status

# 冒烟验证
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8000/
curl -s http://127.0.0.1:8000/health
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8000/static/fonts/001A-Buleku-R.ttf
```

## 4. 已实现路由

| 路由 | 说明 |
|---|---|
| `GET /` | 门户首页（`render_template("index.html")`） |
| `GET /health` | 健康检查，返回 `{"status":"ok","service":"manchu-portal"}` |

## 5. 配置约定（待落地 `.env` / `config.py`）

```ini
MANJU_ENV=dev            # dev | prod | demo
PORT=8000

# 各模型服务地址（前端永远只写 /api/...，由门户转发）
OCR_RECOGNIZE_URL=http://127.0.0.1:1000
OCR_DETECT_URL=http://127.0.0.1:7860
TRANSLATE_URLS=8001,8003,8005
TTS_URL=http://127.0.0.1:7868
ASR_URL=

# 演示保障
DEMO_MODE=off            # off | on（on 时所有推理走预置结果）
CACHE_BACKEND=file       # file | redis
STORAGE_BACKEND=local    # local | minio | oss
```

**硬约定**：
1. 前端**不允许**出现 `localhost:端口`，只用 `/api/<module>/...` 相对路径；
2. 第三方库（jszip / mammoth 等）本地化到 `static/vendor/`，不依赖 CDN；
3. 时区 `Asia/Shanghai`、编码 UTF-8；
4. 满文⇄拉丁规则引擎**全站只有一份实现**（`common/manju_rules.py`）。

## 6. 与本地其它服务联调

| 服务 | 地址 | 启动（本机 Ubuntu 端） |
|---|---|---|
| OCR 识别（远程 OCR） | `http://127.0.0.1:8080` | `source ~/miniconda3/etc/profile.d/conda.sh && conda activate manju_layout && cd ~/ocr_deploy && nohup python server.py --host 0.0.0.0 --port 8080 > /tmp/ocr_server.log 2>&1 &` |
| TTS | `http://127.0.0.1:7868` | `cd ~/shaopf/manchu_TTS && /home/leyesi/miniconda3/envs/GPT/bin/python app.py`（GPT-SoVITS 依赖在 **GPT** 环境）；**已联调通过**，见 [09](./09-满语语音合成TTS模块移植记录.md) |
| 翻译 8001/8003/8005 | Windows 工作站 | 由 `服务启动器.py`（Tkinter）一键启动；跨机访问需把 `localhost` 换成工作站 IP |

> Windows 工作站上的服务若需被本机（Ubuntu）访问，需将服务绑定到 `0.0.0.0` 并用工作站 IP 访问；门户通过 `.env` 中的地址统一配置。

**本机联调的两条硬注意**（都是踩过的坑，详见 [09](./09-满语语音合成TTS模块移植记录.md) 第 8 节）：

1. **三个服务的进程名都含 `app.py`**：定位/停止一律按**解释器绝对路径**匹配（脚本已这么做），切勿 `pkill -f "python app.py"`，会误杀 TTS。
2. **门户必须在其目录下、用绝对路径解释器启动**：`( cd ~/shaopf/manchu_demo && .venv/bin/python app.py )`，否则相对资源（模板/字体）解析不到，表现为 curl 返回 000。

排障顺序建议：`ss -lntp | grep :端口` 看端口到底是谁在听 → `pgrep -af <绝对路径>` 看进程 → 再用 `/healthz`、`/health` 探活。
